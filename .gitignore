# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# OS
.DS_Store

# Local-only relational mirror (regenerated each run; the durable history lives in
# data/metrics_history.csv, which IS committed so the CFI series accumulates).
data/warehouse.sqlite

# Locally rendered static memo image is regenerated by the pipeline; keep it out of
# git to avoid binary churn (the live site renders the interactive charts directly).
site/assets/liquidation_map.png

# Editor
.vscode/

# Local Claude tooling / skills (not part of the deliverable)
.claude/
