# Core pipeline dependencies (kept intentionally small and boring).
requests>=2.31
pandas>=2.0
plotly>=5.20
kaleido            # static PNG export for the memo figure (optional; site degrades gracefully)

# Storage is JSON + CSV + a SQLite mirror — deliberately git-friendly so the live
# demo can persist its accumulating history straight in the repo. No warehouse/
# parquet dependency is needed for the PoC.
#
# Airflow is NOT pinned here on purpose: the DAG is a proof-of-concept artifact
# meant to be read/dropped into an existing Airflow deployment. The live demo is
# driven by GitHub Actions calling run_pipeline.py, which only needs the above.
